import React from "react";
import { observer } from "mobx-react-lite";
import { PageLayout } from "../../containers/PageLayout/PageLayout";

export const UserSettings = observer(() => {
        return (
            <PageLayout title="User Settings"></PageLayout>
        );
});